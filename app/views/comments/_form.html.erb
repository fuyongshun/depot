<%= form_for([@product, @product.comments.build]) do |f| %>
  <div class="field">
    <%= f.text_area :body, :rows => 2, :cols => 60 %>
  </div>
  <div class="actions">
    <%= f.submit t('.submit')%>
  </div>   
<% end %>