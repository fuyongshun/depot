<%= form_for(@star) do |f| %>
  <% if @star.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@star.errors.count, "error") %> prohibited this star from being saved:</h2>

      <ul>
      <% @star.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :product_id,t('.product') %><br />
    <%= f.text_field :product_id %>
  </div>
  <div class="field">
    <%= f.label :user_id,t('.user') %><br />
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :grade,t('.grade') %><br />
    <%= f.text_field :grade %>
  </div>
  <div class="actions">
    <%= f.submit t('.submit')%>
  </div>
<% end %>
